// Firebase SDK has been removed. Authentication is handled by Supabase Auth.
// Firestore and Storage are no longer used by the frontend.
//
// This file exists as a stub to avoid import errors from any code paths
// that have not yet been updated. Remove it once all references are cleaned up.

export const isFirebaseConfigured = false;
export const missingFirebaseEnv: string[] = [];
